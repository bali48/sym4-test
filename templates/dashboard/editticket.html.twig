{% extends 'base.html.twig' %}
{%  block body %}
    {{ form_start(form, {'attr': {'id': 'ticket_edit'}}) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-success" value="Update Ticket">
    {{ form_end(form) }}
{% endblock %}